class Repo {

    constructor(repo_id, repo_name, repo_url, repo_language, repo_description) {
        this.repo_id = repo_id;
        this.repo_name = repo_name;
        this.repo_url = repo_url;
        this.repo_language = repo_language;
        this.repo_description = repo_description;
    }

}

export default Repo;